{
  "name": "run-monorepo",
  "description": "⚡run monorepo",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git://github.com/runtools/run"
  },
  "scripts": {
    "lint": "eslint .",
    "test": "(cd core && npm test) && (cd cli && npm test) && (cd common && npm test)"
  },
  "devDependencies": {
    "babel-eslint": "^7.2.3",
    "eslint": "^3.19.0",
    "eslint-config-xo": "^0.18.2"
  },
  "dependencies": {}
}
