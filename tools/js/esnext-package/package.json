{
  "name": "@runtools/js-esnext-package",
  "version": "0.1.0",
  "private": true,
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist && babel src --out-dir dist"
  },
  "babel": {
    "presets": [
      [
        "env",
        {
          "targets": {
            "node": 6
          },
          "loose": true,
          "exclude": [
            "transform-regenerator"
          ]
        }
      ]
    ],
    "plugins": [
      "transform-object-rest-spread"
    ]
  },
  "dependencies": {
    "@runtools/js-package": "^0.1.0",
    "babel-core": "^6.24.0",
    "babel-plugin-transform-object-rest-spread": "^6.23.0",
    "babel-preset-env": "^1.2.2",
    "is-directory": "^0.3.1",
    "run-common": "^0.5.4"
  },
  "devDependencies": {
    "babel-cli": "^6.24.0",
    "prettier": "^0.22.0"
  }
}
